---
import SkillBadge from "@/components/SkillBadge.astro";
import type { SkillCardProps, Skill } from "@/data/skills";
type Props = SkillCardProps;

const { skillGroup, type } = Astro.props;
---

<>
  <div
    class="group flex flex-col justify-between gap-2 border rounded border-neutral-500 p-2"
  >
    <div class="flex flex-col gap-2 md:flex-row md:items-center md:gap-4">
      <p class="text-xl">{type}</p>
    </div>
    {skillGroup.length === 0 && <p>Soon, stay connected 👀...</p>}
    <p class="w-fit px-1 py-1">
      {
        skillGroup.length !== 0 &&
          skillGroup.map((skill: Skill) => <SkillBadge {...skill} />)
      }
    </p>
  </div>
</>
