---
import Layout from "@/layouts/Layout.astro";
import projects from "@/data/projects";
import ProjectCard from "@/components/ProjectCard.astro";
---

<Layout title="Chaturved Degloorkar | Projects">
  <h1 class="text-3xl font-bold py-2">Projects</h1>
  <div class="flex flex-col items-center min-h-lvh">
    {projects.length === 0 && <p>Soon, stay connected 👀...</p>}
    <div class="grid grid-cols-1 gap-8 py-4 sm:grid-cols-2 lg:grid-cols-3">
      {projects.map((project) => <ProjectCard {...project} />)}
    </div>
  </div>
</Layout>
